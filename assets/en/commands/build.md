---
name: build
command_prefix: ezfl
triggers: ["/ezfl:build"]
description: Enter the build phase: select an implementer subagent to execute /opsx:apply and implement tasks one by one
---

Use the `easy-flow:build` skill.
