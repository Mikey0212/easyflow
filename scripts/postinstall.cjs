#!/usr/bin/env node

/**
 * postinstall: print a friendly message after npm install
 */

console.log(`
  ──────────────────────────────────────
  easyflow installed!

  Get started:
    npx @code-happy/easyflow init

  Or if installed globally:
    easyflow init
  ──────────────────────────────────────
`);
